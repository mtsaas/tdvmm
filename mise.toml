[tools]
rust = "1.97"

[tasks.build]
description = "Build the tdvmm binary in release mode (-> ./target/release/tdvmm)"
run = "cargo build --release"

[tasks.install]
description = "Build and install the tdvmm binary to ~/.cargo/bin (must be on PATH)"
run = "cargo install --path . --locked"
