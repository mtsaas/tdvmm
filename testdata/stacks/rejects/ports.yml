# REJECT-CORPUS: published ports -> WARN + STRIP (not a hard reject).
# bake-stack must emit a loud warning and drop the ports mapping from the lock.
services:
  web:
    image: docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
    command: ["sh", "-c", "sleep 1"]
    ports:
      - "8080:80"
