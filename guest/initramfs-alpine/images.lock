# Images baked into the guest container store (closed world: nothing is ever
# pulled at guest runtime). This file is BOTH the authoritative pin list and a
# provenance ledger; prebake_images.sh parses the pin lines and rewrites the
# GENERATED PROVENANCE block on every bake.
#
# ---------------------------------------------------------------------------
# Determinism-phase NOTES (Phase 1)
# ---------------------------------------------------------------------------
# The unit of run-to-run reproducibility for now is the BUILT INITRAMFS
# ARTIFACT: build_rootfs.sh writes initramfs-alpine.cpio.gz and its sha256
# sidecar (.sha256). An identical artifact hash == identical initial guest
# state, which is what deterministic replay will care about. Bit-reproducible
# REBUILDS (SOURCE_DATE_EPOCH / timestamp normalization of apk, image build,
# cpio) are explicitly OUT of Phase-1 scope -- not done here, just noted.
#
# Image content identities below are recorded per image. Registry images carry a
# real upstream digest. Images we repackage/build locally (squashed postgres, the
# service) have NO registry digest, so their content identity is the RESULT
# layer's DiffID (sha256 of the uncompressed layer tar) recorded by prebake.
#
# ONE-TIME EQUIVALENCE RESULT (re-run whenever the pinned postgres digest changes):
#   The squashed postgres filesystem was byte-diffed against the upstream image's
#   flattened filesystem (podman export of each, minus the 3 per-container files
#   podman injects: /etc/hostname,hosts,resolv.conf). Result: IDENTICAL -- 3862
#   entries each, empty `diff -r`. Every bake additionally GATEs config equality
#   (Entrypoint/Cmd/Env incl PGDATA/Volumes); see prebake_images.sh.
#
# ---------------------------------------------------------------------------
# PIN LIST (authoritative; parsed by prebake_images.sh)
# ---------------------------------------------------------------------------
# One entry per line: "<policy> <full-reference-with-digest>"
#   plain  : pull as-is (tiny images kept verbatim), pulled straight into the seed.
#
# As of Phase 2a this file pins ONLY the busybox self-test image (the base 2a
# container guest). WORKLOAD images now live in each stack's compose file and are
# resolved + squashed + baked by guest/bake-stack.sh, which owns the single-layer
# squash policy + config-equivalence gate (vfs stores every layer as a full copy,
# so heavy images -- e.g. postgres, ~1.8 GiB as-pulled -- are repackaged to one
# ~290 MiB layer). The insert-trim stack's postgres pin lives in
# guest/stacks/insert-trim/compose.yml; its bake ledger is guest/stacks/insert-trim/stack.lock.
plain docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616

# >>> GENERATED PROVENANCE (rewritten by prebake_images.sh on each bake) >>>
# podman-version: 5.8.3
# baked-at: 2026-08-02T08:47:29Z  (informational only; NOT a repro anchor -- see NOTES)
#
# busybox   policy=plain   squashed=no   guest-ref=docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
#   upstream=docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616  (content identity = upstream digest)
# <<< GENERATED PROVENANCE END <<<
