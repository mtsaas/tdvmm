# GENERATED by bake_compose.py -- do NOT edit. The only compose
# file the guest ever sees. Images pinned by digest; ports stripped;
# relative RO binds materialized to in-guest paths; project pinned.
name: dvmm_rwbind
services:
  writer:
    command:
    - /bin/sh
    - -c
    - "echo \"DVMM_RW_SEED=$$(cat /data/seed.txt 2>/dev/null)\"\necho \"run-$$(date\
      \ -u +%s)\" > /data/written.txt\necho \"DVMM_RWBIND_OK=$$(cat /data/written.txt\
      \ 2>/dev/null)\"\necho \"hello-volume\" > /work/vol.txt\necho \"DVMM_VOLUME_OK=$$(cat\
      \ /work/vol.txt 2>/dev/null)\"\ni=0\nwhile :; do\n  i=$$((i+1))\n  echo \"DVMM_RW_HEARTBEAT\
      \ iter=$$i data=$$(cat /data/written.txt 2>/dev/null) vol=$$(cat /work/vol.txt\
      \ 2>/dev/null) ts=$$(date -u +%H:%M:%S)\"\n  sleep 3600\ndone\n"
    image: docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
    volumes:
    - /var/lib/dvmm-stack/binds/writer/data:/data:rw
    - work:/work
volumes:
  work: null
