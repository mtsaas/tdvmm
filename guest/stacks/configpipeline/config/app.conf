# deterministic-vmm corpus (configpipeline) seed config.
#
# Materialized into the guest via a relative RW bind (./config -> /etc/app:rw).
# The worker reads the `seed=` token to prove the bind was materialized, then
# writes alongside it to prove the mount is read-write.
mode=corpus
seed=configpipeline-seed-v1
