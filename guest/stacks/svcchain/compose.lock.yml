# GENERATED by bake_compose.py -- do NOT edit. The only compose
# file the guest ever sees. Images pinned by digest; ports stripped;
# relative RO binds materialized to in-guest paths; project pinned.
name: tdvmm_svcchain
services:
  backend:
    depends_on:
      db:
        condition: service_healthy
    entrypoint:
    - /bin/sh
    - /opt/tdvmm/backend-loop.sh
    environment:
      INTERVAL_SECONDS: ${TDVMM_INTERVAL:-3600}
      MAX_ROWS: ${TDVMM_MAXROWS:-1000}
      PGDATABASE: appdb
      PGHOST: db
      PGUSER: postgres
    healthcheck:
      interval: 2s
      retries: 5
      start_period: 60s
      test:
      - CMD-SHELL
      - test -f /tmp/ready
      timeout: 3s
    image: localhost/tdvmm-postgres-57c72fd2a128@sha256:cbf217007d0742829dc120c3ea9cd2621e90eb3adfeaf6684e87ce268a2ca368
    volumes:
    - /var/lib/tdvmm-stack/binds/backend/backend-loop.sh:/opt/tdvmm/backend-loop.sh:ro
    - /run/tdvmm/events:/run/tdvmm/events:rw
  db:
    environment:
      POSTGRES_DB: appdb
      POSTGRES_HOST_AUTH_METHOD: trust
    healthcheck:
      interval: 2s
      retries: 5
      start_period: 60s
      test:
      - CMD-SHELL
      - pg_isready -U postgres -d appdb
      timeout: 3s
    image: localhost/tdvmm-postgres-57c72fd2a128@sha256:cbf217007d0742829dc120c3ea9cd2621e90eb3adfeaf6684e87ce268a2ca368
    shm_size: 256m
    volumes:
    - /var/lib/tdvmm-stack/binds/db/schema.sql:/docker-entrypoint-initdb.d/10-schema.sql:ro
    - /run/tdvmm/events:/run/tdvmm/events:rw
  frontend:
    command:
    - /bin/sh
    - -c
    - 'echo "TDVMM_FRONTEND_UP backend-was-healthy ts=$$(date -u +%H:%M:%S)"

      while :; do sleep 3600; done

      '
    depends_on:
      backend:
        condition: service_healthy
    image: docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
    volumes:
    - /run/tdvmm/events:/run/tdvmm/events:rw
