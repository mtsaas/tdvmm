# GENERATED by bake_compose.py -- do NOT edit. The only compose
# file the guest ever sees. Images pinned by digest; ports stripped;
# relative RO binds materialized to in-guest paths; project pinned.
name: dvmm_health-gate
services:
  dependent:
    command:
    - /bin/sh
    - -c
    - 'echo "DVMM_DEPENDENT_STARTED gate-was-healthy ts=$$(date -u +%H:%M:%S)"

      while :; do sleep 3600; done

      '
    depends_on:
      gate:
        condition: service_healthy
    image: docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
  gate:
    command:
    - /bin/sh
    - -c
    - 'echo "DVMM_GATE_STARTING ts=$$(date -u +%H:%M:%S)"

      sleep 30

      touch /tmp/ready

      echo "DVMM_GATE_READY ts=$$(date -u +%H:%M:%S)"

      while :; do sleep 3600; done

      '
    healthcheck:
      interval: 2s
      retries: 3
      start_period: 60s
      test:
      - CMD-SHELL
      - test -f /tmp/ready
      timeout: 2s
    image: docker.io/library/busybox@sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616
